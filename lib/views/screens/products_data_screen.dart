import 'package:flutter/material.dart';

class ProductsDataScreen extends StatelessWidget {
  Map product;
   ProductsDataScreen({super.key, required this.product});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
